
:- use_module('shapes.pl').


% There is a predicate whose name starts with log that prints 
% a list of shapes. Use the autocomplete to find it!

% Whenever you type a `,` it triggers automatically the autocompletion
% But it can also be triggered manually  with `Ctrl + Space`

main:- 
	Shapes = [rectangle(1,2), circle(2), square(3)]
	
	
	.